{
  "name": "maxrects-packer-monorepo",
  "private": true,
  "packageManager": "npm@11.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.9.0"
  }
}
